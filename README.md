# Satooooo

img2.cpp
画面を4分割(左上、右上、左下、右下)し、それぞれにランダムな位置に点を描画する
左上、右上、右下、左下の順に画像の走査をし、赤色の座標を4つ取得し、線で結ぶ
C++にまだ直せてません

camera.cpp
img2.cppの動画版。今回は自分の都合で赤色ではなく、青色を取得している。
画像走査内にあるbool型は、画面内の全ての座標を取得していると動作が重くなってしまうと思い、1つの座標を取得したら次の画面に行くために追加してみた(検証はしてない)。

12/20
camera.cpp
前回からの変更点
・30フレーム取得したらその後は青色の画素値を取得しないようにした。（cキーを押すとまた30フレーム取得する）
・マウスで頂点を動かせるようにした。

1/11
前回からの変更点
・プログラムをc++に変更。
・EX01ソリューション内に、CamとCam2の2つのプロジェクトをつくり、Camには左カメラを表示させるLeftCamera.cpp、Cam2には右カメラを表示させるRightCamera.cppを入れた。
・変数名の変更。
・30フレームから20フレーム取得に変更。

1/12
前回からの変更点
・1つのプロジェクトで実行できるようにした。

1/18
前回からの変更点
・4つの取得した頂点を、透視変換を用いて640×480のサイズにし、別ウィンドウに表示させるLRcamera_per.cppを作成した。
・これを使って指示棒の位置が分かるのでは？
・現在は同じプロジェクト内で同時に実行しているが、欲しい値だけを別のプロジェクトに持っていき、順次実行できる方法を探している。

1/24
前回からの変更点
・Cursor.cppの作成。
・赤色を取得し、その重心を円で表示する。キーボードの'a'を押すとその重心とマウスカーソルがリンクする。
・マウスの左ボタンを押しながら赤色の重心を移動させると矩形を表示する。そして、左ボタンを離すとスクリーンショットを撮る。
・重心は"Video"から、マウスはウィンドウから座標を取得しているため、ズレがある。